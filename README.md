# 宝塔面板虚拟主机魔方财务对接模块
![](https://i.imgur.com/Cj58XW6.png)

## 准备

1.在服务器上安装好宝塔面板

安装phpMyAdmin 5.0和PHP 7.4.33 ,FTP服务 1.0.49

2.设置phpMyAdmin 5.0 端口为8888，启用公共访问.

3 开放8888端口（防火墙和安全组）

## 安装

1.下载本项目，将baota文件夹放在网站 /public/plugins/servers/目录下.

2.在后台-商品-接口设置中先创建新的分组，模块选择宝塔面板对接模块，名字自拟.

添加新接口，分组就选择刚创建好的那个分组，IP地址和主机名填写宝塔的域名或IP，然后将下方的hash填入宝塔面板上设置的API接口-接口密钥，记得添加白名单IP为你的财务系统服务器IP，若有SSL就打开SSL，端口填写宝塔面板的端口。

3.添加商品，自动开通接口分组选择“宝塔对接模块(主机)”

| 配置项目          | 设定值 / 默认值      | 说明与注意事项                           |
| ------------- | -------------- | --------------------------------- |
| **网站路径**      | `/www/wwwroot` | ⚠️ **警告：** 不懂请勿修改                 |
| **网站端口**      | `80`           | 默认为 80                            |
| **分类 ID**     | `0`            | 默认为 0                             |
| **默认 PHP 版本** | (留空)           | 格式示例：56，72；不懂请留空                  |
| **是否创建 FTP**  | 是              | FTP 地址：xx.xx.xx.xx (请填写公网 IP)     |
| **是否创建数据库**   | 是              |                                   |
| **数据库编码类型**   | `utf8mb4`      | 可选：utf8 \| utf8mb4 \| gbk \| big5 |
| **站点大小**      | `100` MB       | 输入 -1 为不限制                        |
| **数据库大小**     | `100` MB       | 输入 -1 为不限制                        |
| **域名绑定数**     | `2`            | 输入 -1 为不限制                        |
| **网站备份数**     | `1`            | 输入 -1 为不限制                        |
| **数据库备份数**    | `1`            | 输入 -1 为不限制                        |
| **月流量**       | (未指定)          | 输入 -1 为不限制                        |
| **并发数**       | (未指定)          | 输入 -1 为不限制                        |
| **限制网速**      | (未指定)          | 单位 KB，输入 -1 为不限制                  |
| **绑定子目录**     | `0`            | ⚠️ **高危操作**，不建议开启                 |

基于https://my.idcsmart.com/shop/shop_detail.html?id=1481&clientId=632 二次开发
